# ---------------------------------
# AUTENTICAÇÃO E APIs DE SERVIÇOS
# ---------------------------------
# Chave da sua API da OpenAI (Obrigatório)
OPENAI_API_KEY=sk-proj-xxxx

# Chave do seu GitHub Personal Access Token (Obrigatório)
GITHUB_TOKEN=ghp_xxxx

# Client ID do seu projeto Google Cloud (Obrigatório para Login)
GOOGLE_CLIENT_ID=

# Chave da API do Brevo (ex-Sendinblue) (Obrigatório para E-mail)
BREVO_API_KEY=
# E-mail remetente (Obrigatório para E-mail)
SENDER_EMAIL=seu-email@gmail.com

# Secret para validar Webhooks do GitHub (Obrigatório para Webhook)
GITHUB_WEBHOOK_SECRET=

# ---------------------------------
# INFRAESTRUTURA (SUPABASE & REDIS)
# ---------------------------------
# URL do seu projeto Supabase (Obrigatório)
SUPABASE_URL=

# Chave de SERVIÇO (service_role) do Supabase (Obrigatório)
SUPABASE_KEY=eyJhbGciOi...

# URL de conexão direta ao Redis Cloud (Obrigatório para Fila e Cache)
REDIS_URL=

# ---------------------------------
# CONFIGURAÇÕES DA APLICAÇÃO
# ---------------------------------
# URL pública do seu app Heroku (Obrigatório para links em e-mails)
APP_URL=

# Prefixo para as filas do Redis (permite isolar dev/prod no mesmo Redis)
# Deixe em branco para produção ou use "dev_" para local.
RQ_QUEUE_PREFIX=test_

# Modelo principal da LLM para RAG e Roteamento [cite: 139]
LLM_MODEL=gpt-4o

# Configurações de Cache (Opcional, usado por app/main.py)
ENABLE_CACHE=true
CACHE_EXPIRATION=3600

# (Não é mais necessário, pois o embedding_service.py usa "text-embedding-3-small")
# EMBEDDING_MODEL=all-MiniLM-L6-v2